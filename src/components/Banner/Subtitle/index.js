import React from "react";

import { SubtitleSection } from "./style";

const Subtitle = () => (
  <SubtitleSection>
    <span>
      <span className="bold">Answer 1 question</span> to guide what we build
    </span>
  </SubtitleSection>
);

export default Subtitle;
